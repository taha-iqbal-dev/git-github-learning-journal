# Patch Demo

Create patch:

```bash
diff original.txt modified.txt > changes.patch
```

Apply patch:

```bash
patch original.txt changes.patch
```

Purpose:

Apply recorded changes automatically to a file.